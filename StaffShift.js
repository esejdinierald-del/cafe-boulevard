// Removed CSS import and applied inline styles
import React from 'react';

const StaffShift = () => {
    return (
        <div style={{ display: 'flex', flexDirection: 'column', padding: '10px', backgroundColor: '#f0f0f0' }}>
            <h1 style={{ color: '#333' }}>Staff Shift</h1>
            {/* Other components and elements with inline styles */}
        </div>
    );
};

export default StaffShift;